{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "anti-hall",
  "displayName": "Anti-Hallucination Verify-First",
  "version": "0.108.3",
  "description": "Verify-first discipline for Claude Code. SessionStart injects the full Iron-Law + rationalization-table protocol, the always-on SCOPE & FIDELITY discipline (simplest sufficient solution, intent over letter, confirm before expanding scope, match rigor to blast radius, finish asked work / drop nothing), and a skill primer (and re-injects it after compaction, since SessionStart re-fires with source=compact); a per-turn UserPromptSubmit hook adds a short varying nudge. Skills: root-cause debugging, orchestration (non-blocking swarm), ship-it (one lean plan-mode + Workflow-swarm + deadly-loop-gated workflow scaled S/M/L to blast radius), deadly-loop, handover (comprehensive session handoff so a fresh session can resume without re-deriving or guessing anything). Ships api-guard (blocks fabricated stdlib/builtin APIs in code, verified against the installed python3/node; opt-in 3rd-party via ANTIHALL_API_GUARD_THIRDPARTY=1), git-guard (blocks force-push + AI self-credit in commit trailers AND gh pr/issue/release bodies), task discipline, and a monorepo/simple statusline (plus a repo-root AGENTS.md Codex mirror for clone-based use \u2014 not bundled by /plugin install). Also ships an OPT-IN mcp-reaper companion (macOS + Linux) \u2014 a pure-Node background job that kills ONLY orphaned MCP-server processes (parent already died); install via `node plugins/anti-hall/companion/install-reaper.js` (Windows is a documented no-op). All hooks and the statusline are pure Node (built-ins only) and run unchanged on macOS and Linux; Windows is untested and not officially supported. REQUIRES Node.js >= 22 on PATH: hooks are launched as `node <hook>.js`; without Node, the hooks (including the git-guard safety) silently do not run (intentional \u2014 a shell-based preflight cannot reach stock Windows systems, so we omit it by design). Verify with `node --version` before relying on protections. Includes a separate Codex-native port under plugins/anti-hall/codex with a .codex-plugin manifest, Codex hook-subset installer, OMX-oriented skills, context-conservation and ship-it protocols; Claude hook files remain unchanged.",
  "author": {
    "name": "Mohammed Talas",
    "url": "https://github.com/talas9"
  },
  "homepage": "https://github.com/talas9/anti-hall",
  "repository": "https://github.com/talas9/anti-hall",
  "license": "MIT",
  "keywords": [
    "verification",
    "anti-hallucination",
    "quality",
    "discipline",
    "debugging",
    "root-cause",
    "orchestration",
    "deadly-loop",
    "ship-it",
    "code-simplification",
    "technical-debt"
  ],
  "userConfig": {
    "auto_handover_enabled": { "type": "boolean", "title": "Auto Handover · Enabled", "description": "Write an automatic session handover before context runs out.", "default": true },
    "auto_handover_pct": { "type": "number", "title": "Auto Handover · Threshold %", "description": "Context-usage percent that triggers an automatic handover.", "default": 85, "min": 1, "max": 99 },
    "auto_handover_max_tokens": { "type": "number", "title": "Auto Handover · Token ceiling (0 = off)", "description": "Opt-in absolute context-token ceiling that also triggers the handover, whichever of pct/maxTokens fires first; 0 (the default) = no ceiling — the real per-session context window size (85% of it) is the only trigger unless a user explicitly sets this.", "default": 0, "min": 0 },
    "auto_handover_nag": { "type": "boolean", "title": "Auto Handover · Nag reminders", "description": "Remind the user when a handover is due but not yet written.", "default": true },
    "auto_handover_nag_step_pct": { "type": "number", "title": "Auto Handover · Nag step %", "description": "Percent increments between successive handover nags.", "default": 5, "min": 1, "max": 100 },
    "auto_handover_nag_quiet_min": { "type": "number", "title": "Auto Handover · Nag quiet minutes", "description": "Minutes to wait before repeating a handover nag.", "default": 15, "min": 1 },
    "guards_merge_gate": { "type": "boolean", "title": "Guards · Merge-readiness gate", "description": "Enable merge-readiness gate checks before merging.", "default": false },
    "guards_shipit_gate": { "type": "boolean", "title": "Guards · Ship-it gate", "description": "Enable the ship-it workflow gate.", "default": false },
    "guards_output_verify_guard": { "type": "boolean", "title": "Guards · Output-verify guard", "description": "Output-verification guard (blocks unverified completion claims).", "default": true },
    "guards_failure_root_cause_nudge": { "type": "boolean", "title": "Guards · Failure root-cause nudge", "description": "Nudge toward root-cause analysis after a failure.", "default": true },
    "guards_repo_self_drift": { "type": "boolean", "title": "Guards · Repo self-drift check", "description": "Anti-hall's own repo-drift self-check hook.", "default": true },
    "guards_stash_guard": { "type": "boolean", "title": "Guards · Stash guard", "description": "Arm stash-protection warnings in git-guard (also armed per-repo via .anti-hall/protected-stashes).", "default": false },
    "guards_emit_dedupe": { "type": "boolean", "title": "Guards · Emit dedupe", "description": "Deduplicate repeated hook-emit output.", "default": true },
    "version_alerts_anti_hall": { "type": "boolean", "title": "Version Alerts · anti-hall updates", "description": "Alert when a newer anti-hall version is available.", "default": true },
    "version_alerts_claude_cli": { "type": "boolean", "title": "Version Alerts · Claude Code CLI updates", "description": "Alert when a newer Claude CLI version is available.", "default": true },
    "version_alerts_devswarm": { "type": "boolean", "title": "Version Alerts · DevSwarm updates", "description": "Alert when a newer DevSwarm/hivecontrol version is available.", "default": true },
    "updates_quiet": { "type": "boolean", "title": "Updates / Maintenance · Quiet update output", "description": "Suppress update output (for scripted capture).", "default": false },
    "limit_conserve_mode": { "type": "string", "title": "Limit Conservation · Mode", "description": "Force usage-limit conservation on/off, or auto-detect.", "options": ["auto", "on", "off"], "default": "auto" },
    "limit_conserve_threshold": { "type": "number", "title": "Limit Conservation · Threshold %", "description": "Usage percent that triggers conservation mode.", "default": 85, "min": 1, "max": 99 },
    "jev_enabled": { "type": "boolean", "title": "Jev · Enabled", "description": "Enable the opt-in Jev semantic decision engine.", "default": false },
    "jev_transport": { "type": "string", "title": "Jev · Transport", "description": "Vercel AI Gateway passthrough (default) or a direct TypeSafe API call.", "options": ["vercel", "typesafe"], "default": "vercel" },
    "jev_judge_model": { "type": "string", "title": "Jev · Judge model", "description": "Model used for speculation-judge / jev-triage LLM calls.", "default": "claude-haiku-4-5" },
    "jev_semantic_judge": { "type": "boolean", "title": "Jev · Semantic speculation judge", "description": "Enable the semantic speculation-judge hook.", "default": false },
    "jev_budget_mode": { "type": "string", "title": "Jev · Budget mode", "description": "Jev spend: no limit, or warn when over budget, never auto-disables.", "options": ["unlimited", "watch"], "default": "unlimited" },
    "jev_budget_usd_per_day": { "type": "number", "title": "Jev · Daily budget (USD)", "description": "Daily USD spend threshold, used only when budget mode is \"watch\". Leave unset for no daily threshold." },
    "jev_budget_usd_per_week": { "type": "number", "title": "Jev · Weekly budget (USD)", "description": "Weekly USD spend threshold, used only when budget mode is \"watch\". Leave unset for no weekly threshold." },
    "jev_weekly_notice": { "type": "boolean", "title": "Jev · Weekly scorecard notice", "description": "Once-a-week SessionStart scorecard notice naming one integration worth promoting or turning off (Jev enabled only).", "default": true },
    "jev_budget_min_credit_usd": { "type": "number", "title": "Jev · Min credit alert (USD)", "description": "Optional: warn (once a day, budget.mode=watch only) when the gateway credit balance drops below this USD amount." },
    "devswarm_hivecontrol": { "type": "string", "title": "DevSwarm · hivecontrol path", "description": "Explicit path to the hivecontrol CLI binary (default: PATH lookup).", "default": "" },
    "devswarm_supervisor_mode": { "type": "string", "title": "DevSwarm · Supervisor mode", "description": "Force the DevSwarm supervisor context on/off, or auto-detect.", "options": ["auto", "on", "off"], "default": "auto" },
    "devswarm_required_gates": { "type": "string", "title": "DevSwarm · Required merge gates (csv)", "description": "Merge gates required for DevSwarm tasks.", "default": "done,merged,tests_passed" },
    "devswarm_inbox_cmd": { "type": "string", "title": "DevSwarm · Inbox command", "description": "Consumer-configured command to read pending mesh messages (no built-in default).", "default": "" },
    "devswarm_auto_archive_mode": { "type": "string", "title": "DevSwarm · Auto-archive", "description": "Auto-archive finished workspaces, needs DevSwarm >= 2.5.3.", "options": ["on", "dry-run", "off"], "default": "on" },
    "devswarm_auto_archive_idle_min": { "type": "number", "title": "DevSwarm · Auto-archive idle minutes", "description": "Minutes idle before a finished workspace is eligible for auto-archive.", "default": 30, "min": 5 },
    "devswarm_auto_archive_max_per_sweep": { "type": "number", "title": "DevSwarm · Auto-archive max per sweep", "description": "Max workspaces auto-archived in one sweep.", "default": 3, "min": 1, "max": 20 },
    "statusline_base": { "type": "string", "title": "Statusline · Base command", "description": "Shell command run as the line-1 base in consolidated statusline mode.", "default": "" },
    "statusline_no_email": { "type": "boolean", "title": "Statusline · Hide email", "description": "Suppress the email segment in the statusline.", "default": false },
    "codex_nudge_enabled": { "type": "boolean", "title": "Codex Nudge · Codex hand-off nudge", "description": "Enable the Codex hand-off nudge hook.", "default": true },
    "defects_default_proj": { "type": "string", "title": "Defects · Default project tag", "description": "Default project tag used when filing an anti-hall defect (max 64 chars).", "default": "" }
  }
}
