{
  "name": "anti-hall-tests",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end test suite for the anti-hall plugin hooks (zero runtime deps; Node built-in node:test).",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
