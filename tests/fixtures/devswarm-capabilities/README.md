# hivecontrol help fixtures

| File | Provenance |
|---|---|
| `hivecontrol-2.5.2-workspace-help.txt` | REAL — captured verbatim from `hivecontrol workspace --help` on DevSwarm 2.5.2. |
| `*-2.5.3-*.UNVERIFIED.txt` | NOT captured. Written from the DevSwarm 2.5.3 release notes (archive = recoverable, preserves the worktree; delete = archived-only, protects the Primary, refuses uncommitted work). Exact verb syntax, argument names and flags are GUESSES — re-capture from a real 2.5.3 install and replace these files after upgrading. |
