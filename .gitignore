plugins/anti-hall/statusline/demo-wrapper.sh

# Local working notes / session handoff — never ship
CONTINUE-HERE.md
.anti-hall/
# anti-hall task-work progress file (tasklist-guard) — local state, never ship
.anti-hall-progress.md

# Generated knowledge graph (large, machine-specific paths in metadata) — never commit
graphify-out/

# Per-project Claude settings — contain machine-absolute paths, never commit
.claude/settings.json
.claude/settings.local.json
.claude/settings.json.bak-antihall

# Demo recording intermediate artifacts — regeneratable from demo.sh
assets/demo/*.cast

# eval raw per-response results + run logs — regenerable from run.js, bulky, machine-run
eval/results*.json
eval/*.log
