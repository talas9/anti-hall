plugins/anti-hall/statusline/demo-wrapper.sh

# Local working notes / session handoff — never ship
CONTINUE-HERE.md
HANDOVER-*.md
.anti-hall/
# anti-hall task-work progress file (tasklist-guard) — local state, never ship
.anti-hall-progress.md
# anti-hall fix/task history ledger (cause+fix per completed task) — local, Obsidian-readable
.anti-hall-history.md

# Generated knowledge graph (large, machine-specific paths in metadata) — never commit
graphify-out/

# Per-project Claude settings — contain machine-absolute paths, never commit
.claude/settings.json
.claude/settings.local.json
.claude/settings.json.bak-antihall

# Demo recording intermediate artifacts — regeneratable from demo.sh
assets/demo/*.cast

# eval raw per-response results + run logs — regenerable from run.js, bulky, machine-run
eval/results*.json
eval/scope-results*.json
eval/*.log
# local project working notes — never ship to the public repo
CLAUDE.md

# macOS Finder metadata — never commit
.DS_Store
.omc/
docs/*-session-handoff.md
build/

# SkillOpt staging / helper scripts — never commit
.skillopt-sleep/
# SkillOpt deployment manifest — personal paths, never commit
SKILLOPT-DEPLOYMENT.md
.devswarm-temp/
# DevSwarm workspace config — machine-local (references gitignored settings.local.json), never ship
.devswarm/
# ad-hoc scratch prompts — never ship
sanity_prompt.txt
